# Leave empty for local development so Vite proxies API requests to localhost:8000.
# Set this in Vercel for Production deployments; it is public browser configuration.
VITE_API_BASE_URL=https://api.geo-tko.online
